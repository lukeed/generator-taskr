Description:
  Scaffold out a Fly plugin

Example:
  yo fly

  This will create:
    index.js: Template for a transformer plugin.
    README.md: Customized template.
    .travis.yml
    .editorconfig
    .eslintrc
    .gitignore

    package.json: Necessary dev dependencies, and following scripts:
      ・lint: Lint your code via ESLint.
      ・setup: Install dependencies and run lint/tests.
      ・test: Lint / test your code.
      ・deploy: Test, push to origin master and publish the package.
      ・test-ci: Used by .travis.yml to run tests.

    [CHANGELOG.md] Optional
